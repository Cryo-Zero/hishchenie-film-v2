# Update package archive

This directory is reserved for historical and future HISHCHENIE / THEFT update packages.

`MANIFEST.md` records every recovered package name, byte size and SHA-256 checksum so copies can be verified bit-for-bit.

Large binary ZIP payloads are not duplicated into the production repository. The reserve repository is the designated archive location.
